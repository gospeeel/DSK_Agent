# Переход frontend с моков на backend

Актуально на 13 сентября 2026 года. HTTP-контракты описаны в [FRONTEND_INTEGRATION.md](./FRONTEND_INTEGRATION.md).

## Реализовано

- Cookie-аутентификация, роли `user`, `manager`, `supervisor`, выход и очистка server-state.
- Клиенты, обращения, история сообщений, сделки и их допустимые переходы статусов.
- Каталог ЖК, корпусов, квартир и этапов строительства; создание записей менеджером и редактирование руководителем без удаления.
- Семь детерминированных шаблонов этажей: один корпус и этаж всегда получают ту же совместимую геометрию, фактические ID/цены/статусы приходят из backend.
- Карточка объекта с квартирами, ходом строительства, парковками, кладовыми и имитацией ERP: события, остатки материалов, график ЖБИ.
- Реестр КП: серверный расчёт, генерация AI, версии, согласование/отклонение руководителем, полноценный кириллический PDF с квартирой, планировкой, доппозициями и условиями, SMTP-отправка вложения и запись результата доставки.
- Парковка и кладовая выбираются из свободных позиций корпуса, валидируются backend, входят в итоговую цену и сохраняются в версии КП/PDF со снимком цены.
- Версионируемая матрица скидок по корпусу и роли. При отсутствии действующей записи используется конфигурационный лимит backend.
- Приватный анализ переписки менеджера: факты, возражения и черновик ответа не попадают в клиентский чат без действия менеджера.
- Конкурентный анализ с ценой за м², районом, источником и датой наблюдения.
- Клиентские уведомления о строительных рисках, staff-напоминания и серверный аудит AI-действий.
- AI получает ERP-риски через `construction.events.get`. Точная дата сдачи разрешена в prompt только при `exact_delivery_date_allowed=true`.
- AI-интерфейс и HTTP endpoint доступны только сотрудникам; клиентский маршрут закрыт guard-ом.
- Идемпотентный `backend/seeds/demo.sql` покрывает роли и все поддерживаемые состояния основных сущностей; `backend/scripts/seed-demo.sh` применяет его к текущему Docker volume.
- Рабочие маршруты frontend не используют fallback на mock-данные. Удалены legacy `mock-api`, наборы клиентов/задач/КП и отдельный mock-каталог квартир.

Frontend соблюдает FSD: страницы компонуют widgets/features, запросы и бизнес-состояние находятся в `model`/`api`/`lib`, UI получает данные и события. Pinia используется для состояния сессии/UI, TanStack Query — для server state.

## Покрытие endpoint-ов

Все сценарии, доступные в текущем интерфейсе, подключены к `user-server` или `staff-server`; мокового сетевого контура нет. Однако не каждый вспомогательный backend-route имеет отдельный frontend-сценарий:

- отсутствует supervisor-экран создания сотрудников (`POST /api/auth/register` staff-server);
- нет UI редактирования ЖК, квартир, этапов строительства и парковок/кладовых, хотя соответствующие `PUT`-маршруты есть;
- одиночные GET для обращения, сделки, КП, ЖК и этапа не вызываются отдельно, потому что текущие экраны получают эти сущности из списковых запросов.

Это не блокирует удаление mock-данных: ни один активный route приложения от них не зависел.

## Что ещё требуется

- [ ] Добавить просмотр истории доставок КП и повторную отправку из outbox. Сейчас каждая попытка записывает `sent`/`failed`, но автоматического retry-процесса нет.
- [ ] Добавить supervisor-интерфейс создания сотрудников и редактирования ЖК, квартир, этапов строительства и парковок/кладовых. Редактирование конкурентов и корпусов уже подключено.
- [ ] Расширить фильтры больших реестров, человекочитаемое отображение произвольных `preferences` и быстрый переход из клиента в нужную переписку.
- [ ] Удалить оставшиеся неиспользуемые дорефакторинговые UI-слайсы, которые не входят в router; они уже отключены от данных и не попадают в рабочий bundle.
- [ ] Добавить live e2e с RabbitMQ + Agent Service + SMTP. PostgreSQL уже проверяется полным локальным seed-набором; Playwright-тесты пока используют сетевые fixtures.
